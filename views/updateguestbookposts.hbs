 <form method="post" action="/updateguestbookposts/{{guestbook.id}}">
        <h2>Post blog posts</h2>
        <div class="contact-box">
            title: <input type="text" name="title" required="required"  value="{{guestbook.title}}">
        </div>
        <div class="contact-box">
            Content: <textarea name="content" required="required">{{guestbook.content}}</textarea>
        </div>
        <div class="contact-box">
            <input type="submit" value="Post">
        </div>
    </form>